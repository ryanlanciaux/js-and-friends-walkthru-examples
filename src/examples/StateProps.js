import React from "react";

/*
1) Make a Movie component and take a title prop
  Render this component and pass in the title.
  Now add a couple more instances of this component

2) Add local state to the top level component [count , setCount]
   overwrite the return statement to render the count
   Add a button for updating the state 

3) Make a Button component and receive onWatch callback as prop
   Update the Movie component to receive the callback as a prop
   Movie component should return Title, and the new Button component
   Rename count to moviesWatched, make a handler for updating the movies count
*/

function StateProps() {
  return <p>Hi</p>;
}

export default StateProps;
